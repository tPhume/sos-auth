version: '3.7'
services:
  psql:
    build: ./psql
    container_name: sos-psql
    ports:
      - "5432:5432"
  redis:
    container_name: sos-redis
    image: redis:5.0.8
    ports:
      - "6379:6379"
  auth:
    build: .
    container_name: sos-auth
    environment:
      JWT_SECRET: example-secret
      PSQL_URI: postgresql://docker:password@sos-psql:5432/docker
      REDIS_ADDR: sos-redis:6379
    ports:
      - "4356:4356"